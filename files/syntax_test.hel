16
/// documentation
// comment
const thrd = import("thread")
const coroutine = import("coroutine")

const main = func() void {
	const x = -(+(1, *(2, 7)))
	const y = 6
	const z = 15
	const array = [6]i32 {1,2,3,4,5,6}
	
	==(x, 15)
	==(>(+x, 0), true)
	==(<(-y, 0), true)
	==(<(-+y, 0))
	
	// Not like '+(1 4)' because of left definition tokens like '[':
	// +(1 []5)
	// Is '1' indexed or is 5 an array?
	+(1, 4)
	
	==(<<(1, 1), 2)
	==(<<(1, 2), 4)
	
	// Anything non-zero is true
	==(and(1, 5), true)
	==(bool, u8)
	==(signed(bool), i8)
	==(and(true, or(false, true, false)), true)
}

const component = union(i32) {
	pub transform: struct {
		pub x: f64
		pub y: f64
		pub z: f64
	},
	pub orientation: struct {
		pub x: f64
		pub y: f64
		pub z: f64
		pub w: f64
	},
	pub mesh_3d: struct {
		pub data: ArrayList(Tri)
	}
}